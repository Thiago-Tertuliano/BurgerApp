# ===== CONFIGURAÇÕES DO SERVIDOR =====
# Porta onde o servidor da API irá rodar
# Se não definida, usa 8080 como padrão
PORT=8080

# ===== CONFIGURAÇÕES DO BANCO DE DADOS POSTGRESQL =====
# Host do banco de dados PostgreSQL
# Se não definido, usa localhost como padrão
DB_HOST=localhost

# Porta do banco de dados PostgreSQL
# Se não definida, usa 5432 como padrão
DB_PORT=5432

# Usuário do banco de dados PostgreSQL
# Se não definido, usa postgres como padrão
DB_USER=postgres

# Senha do banco de dados PostgreSQL
# Se não definida, usa postgres como padrão
DB_PASSWORD=postgres

# Nome do banco de dados PostgreSQL
# Se não definido, usa hamburgueria como padrão
DB_NAME=hamburgueria

# ===== CONFIGURAÇÕES DE SEGURANÇA =====
# Chave secreta para JWT (JSON Web Tokens)
# Usado para autenticação e autorização
# IMPORTANTE: Altere esta chave em produção!
JWT_SECRET=seu_jwt_secret_aqui 